codecov:
  notify:
    after_n_builds: 3
    wait_for_ci: true
coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%

flags:
  ui:
    paths:
      - ui/src
  server:
    paths:
      - server/api
  python:
    paths:
      - src

ignore:
  - "tests/**"
  - "docs/**"
  - "scripts/**"
  - "**/node_modules/**"
